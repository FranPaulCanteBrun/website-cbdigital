---
/**
 * Página Sobre Mí - CB Digital
 * 
 * Bio profesional y humana.
 */

import BaseLayout from '@/layouts/BaseLayout.astro';

// Cargar traducciones
const pagesEs = await import('@/locales/es/pages.json');
const pagesEn = await import('@/locales/en/pages.json');
const commonEs = await import('@/locales/es/common.json');
const commonEn = await import('@/locales/en/common.json');

// Leer cookie del idioma en el servidor
const localeCookie = Astro.cookies.get('innovatac-locale');
const serverLocale = (localeCookie?.value === 'es' || localeCookie?.value === 'en') 
  ? localeCookie.value as 'es' | 'en'
  : 'es';

const currentLocale = serverLocale;
const pages = currentLocale === 'es' ? pagesEs.default : pagesEn.default;
const common = currentLocale === 'es' ? commonEs.default : commonEn.default;

const bio = {
  es: {
    human: 'Soy Franco Paul Cantero Brunelli, y me dedico a crear soluciones digitales que realmente funcionen. Creo que la tecnología debe ser simple y útil, no complicada. Mi objetivo es ayudarte a hacer crecer tu negocio sin que tengas que entender términos técnicos complicados.',
    technical: 'Trabajo creando sitios web, aplicaciones y programas. Uso las herramientas y métodos más modernos, pero siempre pensando en que el resultado final sea algo fácil de usar para vos. No me importa tanto la tecnología que uso, sino que funcione bien y te sirva.',
    ai: 'Estudié Inteligencia Artificial y sé cómo usarla para ayudar a tu negocio. Puedo automatizar tareas repetitivas, crear imágenes, analizar información y enseñarte a usar la IA de forma práctica. Todo esto te lo explico en palabras simples, sin complicarte.',
    philosophy: 'Creo en escucharte, entender qué necesitás realmente y crear soluciones que funcionen. No me gusta complicar las cosas. Prefiero hacer algo simple que funcione bien, antes que algo complejo que nadie entienda.',
  },
  en: {
    human: 'I am Franco Paul Cantero Brunelli, and I create digital solutions that actually work. I believe technology should be simple and useful, not complicated. My goal is to help you grow your business without having to understand complicated technical terms.',
    technical: 'I work creating websites, applications and programs. I use the most modern tools and methods, but always thinking that the final result should be something easy to use for you. I don\'t care as much about the technology I use, but that it works well and serves you.',
    ai: 'I studied Artificial Intelligence and know how to use it to help your business. I can automate repetitive tasks, create images, analyze information and teach you to use AI in a practical way. I explain everything in simple words, without complicating things.',
    philosophy: 'I believe in listening to you, understanding what you really need and creating solutions that work. I don\'t like to complicate things. I prefer to do something simple that works well, rather than something complex that no one understands.',
  },
};
---

<BaseLayout
  title={`${pages.about.title} - ${common.site.name}`}
  description={currentLocale === 'es' ? bio.es.human : bio.en.human}
>
  <div class="py-16 bg-light-bg-primary dark:bg-dark-bg-primary">
    <div class="container-custom mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-3xl mx-auto">
        <div class="text-center mb-12">
          <h1 class="text-4xl md:text-5xl font-bold text-light-text-primary dark:text-white mb-4">
            {pages.about.title}
          </h1>
          <p class="text-xl text-light-text-secondary dark:text-[#f5f5f5]">
            {pages.about.subtitle}
          </p>
        </div>

        <div class="space-y-8">
          <!-- Foto de perfil -->
          <div class="flex justify-center mb-8">
            <img
              src="/images/foto-franco.jpg"
              alt="Franco Paul Cantero Brunelli - Desarrollador de Software"
              class="w-48 h-48 rounded-full object-cover border-4 border-accent shadow-lg"
            />
          </div>

          <!-- Párrafo humano -->
          <section>
            <p class="text-lg text-light-text-primary dark:text-white leading-relaxed">
              {currentLocale === 'es' ? bio.es.human : bio.en.human}
            </p>
          </section>

          <!-- Párrafo técnico -->
          <section>
            <h2 class="text-2xl font-bold text-light-text-primary dark:text-white mb-4">
              {currentLocale === 'es' ? 'Qué es lo que hago' : 'What I do'}
            </h2>
            <p class="text-lg text-light-text-primary dark:text-white leading-relaxed">
              {currentLocale === 'es' ? bio.es.technical : bio.en.technical}
            </p>
          </section>

          <!-- Inteligencia Artificial -->
          <section>
            <h2 class="text-2xl font-bold text-light-text-primary dark:text-white mb-4">
              {currentLocale === 'es' ? 'Inteligencia Artificial' : 'Artificial Intelligence'}
            </h2>
            <p class="text-lg text-light-text-primary dark:text-white leading-relaxed">
              {currentLocale === 'es' ? bio.es.ai : bio.en.ai}
            </p>
          </section>

          <!-- Filosofía -->
          <section>
            <h2 class="text-2xl font-bold text-light-text-primary dark:text-white mb-4">
              {currentLocale === 'es' ? 'Filosofía de trabajo' : 'Work philosophy'}
            </h2>
            <p class="text-lg text-light-text-primary dark:text-white leading-relaxed">
              {currentLocale === 'es' ? bio.es.philosophy : bio.en.philosophy}
            </p>
          </section>
        </div>
      </div>
    </div>
  </div>
</BaseLayout>

